/// Bản mặc định (Android/Windows) — nền tảng chưa hỗ trợ tải file trực
/// tiếp trong bản này; trả về false để nơi gọi báo cho người dùng biết.
bool saveTextFile(String filename, String content) => false;
